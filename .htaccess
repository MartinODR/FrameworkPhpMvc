#Este Script es para eliminar la de la url la necesidad de escribir /public/

<IfModule mod_rewrite.c>
    
    RewriteEngine on
    
    RewriteRule ^$ public/ [L]
    
    RewriteRule (.*) public/$1 [L]

</IfModule>